# 系统用户重复手机号提示

## 问题

新增系统用户时，服务层只校验用户名，重复的有效手机号会在数据库唯一索引处失败，并将 MyBatis SQL 异常直接返回管理端。

## 修复

- `SysUserServiceImpl.saveUser` 在插入前查询全局有效手机号；命中时抛出业务消息“该手机号已存在”。
- 保留数据库唯一索引作为并发写入的最终约束。
- 编辑接口已禁止修改手机号，本次不改变其行为。

## 验收

- 新增重复手机号时不执行用户插入或角色写入。
- 管理端显示业务提示，不再显示数据库 SQL 异常。
