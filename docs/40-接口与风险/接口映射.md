# 接口映射

> 证据：Gateway/Nacos 路由、Boot context、三端请求文件与后端 Controller；检查于 2026-09-06；置信度：已验证（静态映射），运行态未知。

## 路径规则

| 模式 | 前端源码路径 | 实际请求示例 | 后端入口 |
|---|---|---|---|
| 微服务 | `/product/goodsspu/page` | `/product/goodsspu/page` | Gateway 转发到 product，Controller `/goodsspu` |
| 单体 | `/product/goodsspu/page` | `/boot/goodsspu/page` | Boot context `/boot` + Controller `/goodsspu` |

管理端和 UniApp 都会在 `VITE_OPEN_BOOT=true` 时去掉第一个业务域段。排查 404 时先确认运行模式与最终 URL。

## 管理端覆盖

| 前端模块 | 主要前缀 | 后端模块/Controller | 静态状态 | 风险 |
|---|---|---|---|---|
| `api/core/auth.ts` | `/auth/token` | `aryn-auth` ToB token | 已覆盖 | refresh token 行为需运行验证 |
| `api/core/menu.ts` | `/upms/menu` | `aryn-upms-biz` Menu | 已覆盖 | 同时依赖角色/租户菜单数据 |
| `api/upms/*` | `/upms/*` | `aryn-upms-biz` | 已覆盖 | 资源名存在 `sysrole` 等历史命名 |
| `api/user/*` | `/user/*` | `aryn-user-biz/controller/admin` | 已覆盖 | 会员资料 DTO、权限和租户隔离已加固 |
| `api/product/*` | `/product/*` | `aryn-product-biz/controller/admin` | 已覆盖 | SPU/SKU/规格路径需按 Boot 改写 |
| `api/order/*` | `/mall-order/*` | `aryn-order-biz/controller/admin` | 已覆盖 | 订单状态和统计 SQL需联调 |
| `api/pay/*` | `/pay/*` | `aryn-pay-biz` | 已覆盖 | 回调/退款依赖外部支付配置 |
| `api/promotion/*` | `/promotion/*` | `aryn-promotion-biz/controller/admin` | 已覆盖 | 页面装修、秒杀和折扣均需跨端联调 |
| `api/delivery/*` | `/mall-order/delivery/*` | Order `Delivery*Controller` | 静态覆盖 | 配送表租户白名单已双模式补登（boot `application.yml` + cloud Nacos）；配送资格/守卫专项测试已补齐 52 例 |
| `api/message/*` | `/message/admin\|staff/**`、`/message/ws` | `aryn-message-biz` Admin/Staff Controller、WebSocket | 已覆盖 | 依赖 TOB 身份、动态菜单和角色权限；WebSocket 需双模式联调 |
| `api/gen/*` | `/gen/*` | `aryn-visual/aryn-generator` | 已覆盖 | 独立服务与 Boot 扫包均需确认 |
| `api/home/*` | 多域统计 | product/order/user/promotion 统计 Controller | 部分聚合 | 无独立 home 后端，依赖多个域可用 |

## UniApp 覆盖

| 前端模块 | 主要前缀 | 后端模块/Controller | 静态状态 | 风险 |
|---|---|---|---|---|
| `api/auth.ts` | `/auth/toc-token` | `aryn-auth` ToC token | 已覆盖 | 微信 app-id/platform headers 必需 |
| `api/user/*` | `/user/*`、`/mall-user/app/member/*` | user app Controller | 已覆盖 | 固定租户 ID 配置需部署确认 |
| `api/product/*` | `/product/app/*` | product app Controller | 已覆盖 | 部分公开接口在 security ignore 中 |
| `api/order/*` | `/mall-order/app/*` | order app Controller | 已覆盖 | 下单跨商品、券、支付和事务 |
| `api/pay/*` | `/pay/app/*` | `AppPayOrderController` | 已覆盖 | 平台支付能力和回调未运行验证 |
| `api/promotion/*` | `/promotion/app/*` | promotion app Controller | 已覆盖 | 拼团/领券需租户和幂等校验 |
| `api/message/*` | `/message/app/notice/**`、`/message/app/conversation/**`、`/message/ws` | Message App Controller、WebSocket | 已覆盖 | 强制 TOC 身份；断线后按 HTTP 游标补拉 |
| `api/distribution/*` | user + promotion app | user/promotion Controller | 分域覆盖 | 分销身份与佣金数据跨模块 |
| `api/upms/*` | `/upms/dictvalue|file|sms` | upms app/公共 Controller | 已覆盖 | uploadFile 走独立请求链路 |
| `api/promotion/pageDesign.ts` | `/promotion/app/pagedesign` | `AppPageDesignController` | 已发布快照/预览 | `pageContent` 为对象；旧字符串内容由迁移器兼容 |
| `api/upms/tenant.ts` | `/upms/app/tenant/shop-info` | `AppTenantController` | 当前租户门店公开 VO | 仅返回 `id/name/logoUrl/address/siteUrl/phone`；旧 `/tenant/list` 需要 `upms:systenant:page` 权限 |
| `api/product/spu.ts` | `/product/app/goodsspu/list/{ids}` | `AppGoodsSpuController` | 装修商品批量补全 | 公开查询只返回 `status='1'` 商品，避免装修展示下架商品 |
| `api/promotion/groupBuyActivity.ts` | `/promotion/app/groupbuy/activity/*`、`/promotion/app/groupbuy/record/page` | GroupBuy app Controller | 限时活动与拼团详情 | 活动详情及公开参团记录加入 Boot/Cloud 匿名白名单，写操作仍要求登录 |
| `api/delivery.ts`、`api/order/delivery.ts` | `/mall-order/app/delivery/**`、`/mall-order/app/order/**` | `AppDeliveryTask/TripController`、`AppOrderDeliveryController` | 静态覆盖 | `VITE_OPEN_BOOT=true` 时由 `rewriteBootUrl` 改写为 `/boot/app/...`；需在真实 Gateway/Boot 环境验证鉴权和租户拦截 |
| `api/promotion.ts` | `/promotion/app/seckill/**`、`/promotion/app/discount/**` | `AppSeckillController`、`AppDiscountController` | 静态覆盖 | 秒杀写操作要求登录；公开读取白名单和真实价格链路待联调 |

## 配送与新增营销接口

| 能力 | Cloud 源码路径 | 后端 Controller | 当前结论 |
|---|---|---|---|
| 配送管理 | `/mall-order/delivery/task|staff|trip|area|warehouse-config|region/tree/**` | Order Admin `Delivery*Controller` | 管理端路径与 Gateway 路由一致 |
| 配送员执行 | `/mall-order/app/delivery/task|trip/**` | `AppDeliveryTaskController`、`AppDeliveryTripController` | UniApp `api/delivery.ts` 已统一 `/mall-order/app/delivery` 前缀，Boot 模式由 `rewriteBootUrl` 改写 |
| 客户配送进度 | `/mall-order/app/order/{orderId}/delivery-progress` | `AppOrderDeliveryController` | `api/delivery.ts` 与 `api/order/delivery.ts` 均已带 `/mall-order` 首段，路径已对齐 |
| 秒杀管理/公开端 | `/promotion/seckill/activity/**`、`/promotion/app/seckill/**` | Seckill Admin/App Controller | 静态匹配 |
| 折扣管理/公开端 | `/promotion/discount/activity/**`、`/promotion/app/discount/**` | Discount Admin/App Controller | 静态匹配 |

## 会员接口与订单契约

| 动作 | 路径/接口 | 约束 |
|---|---|---|
| 当前会员权益 | `GET /mall-user/app/member/benefits` | 只读取 token 当前用户和启用等级；返回最优折扣、积分倍率、免邮及专属券模板集合 |
| 可见等级与权益 | `GET /mall-user/app/member/levels`、`GET /mall-user/app/member/level-benefits` | C 端不返回禁用等级或禁用权益 |
| 会员资料/密码 | `/mall-user/app/userinfo/**` | 资料更新强制使用 token 用户 ID；密码修改校验当前密码和两次新密码，统一使用 BCrypt |
| 订单计价 | 用户域 `RemoteMallUserService.getMemberBenefits` | 先应用会员折扣，再校验和分摊当前用户优惠券；免邮最后将运费清零 |
| 订单完成成长 | `OrderCompleteEvent` -> 用户域监听器 | 事件携带实付商品金额及下单时固化的积分倍率，按订单唯一键幂等处理 |
| 等级专属券 | 营销域 `RemoteCouponUserService` | `benefitValue` 作为券模板 ID，首次进入等级按“等级 ID:权益 ID”来源幂等发券 |

## 消息中心接口

| 身份/能力 | 路径 | 关键约束 |
|---|---|---|
| 管理员通知 | `/message/admin/notice/**` | 草稿发布后正文不可编辑；发布/撤回按 `message:notice:*` 权限控制 |
| 工作人员收件箱 | `/message/staff/notice/**` | 当前 TOB 本人收件记录；头部铃铛不要求通知管理权限 |
| 工作人员客服 | `/message/staff/conversation/**`、`/message/staff/agent/**` | 自动分配优先、无可用客服进入待领取；`agent/candidates` 只返回具备 UPMS 客服/主管权限的工作人员，`agent/config/{staffId}` 回显可选配置，保存配置仍由 `message:service:agent` 控制 |
| 工作人员私信 | `/message/staff/direct` | 同租户 `SYS_USER` 一对一，禁止群聊且不占客服负载 |
| 会员通知/客服 | `/message/app/notice/**`、`/message/app/conversation/**` | 当前 TOC 会员；默认队列一个活跃会话，关闭 24 小时内重开 |
| 实时推送 | `/message/ws` | 握手身份由 Sa-Token session 确定；事件只含 ID/序号，HTTP 返回才是发送成功依据 |
| 业务通知 | RocketMQ `MESSAGE_SEND_COMMAND_TOPIC` | 来源唯一键与收件人唯一键双幂等，安全卡片和跳转参数均为 JSON 对象 |

Cloud Gateway 为 `/message/ws/**` 配置 `lb:ws://aryn-message-biz`，普通 `/message/**` 使用 HTTP 路由；Boot 模式由现有前端路径改写直接连接 `/boot/ws` 或 `/boot/**`。真实 Gateway、Boot WebSocket 握手仍需在集成环境验证。

## 页面装修接口

| 动作 | 管理端路径 | 权限 | 公开端路径/约束 |
|---|---|---|---|
| 编辑详情/保存草稿 | `GET /pagedesign/{id}/editor`、`PUT /pagedesign/{id}/draft` | `promotion:pagedesign:edit` | 草稿不公开，保存携带 `draftRevision` |
| 预览 | `POST /pagedesign/{id}/preview-token` | `promotion:pagedesign:edit` | `GET /app/pagedesign/preview/{token}`，令牌绑定租户/页面/修订号并有 TTL |
| 发布/下线 | `POST /pagedesign/{id}/publish|unpublish` | `promotion:pagedesign:publish` | App 只读取当前发布版本 |
| 历史/回滚 | `GET /pagedesign/{id}/versions`、`POST /pagedesign/{id}/versions/{versionId}/rollback` | 查询用 `get`，回滚用 `promotion:pagedesign:rollback` | 回滚生成新快照 |
| 模板 | `/pagedesign/templates` | 查询用 `get`，写操作用 `promotion:pagedesign:template` | 无公开写入口 |
| 首页/微页面 | 管理端按页面 ID 编辑 | 原有查看/编辑权限 | `GET /app/pagedesign`、`GET /app/pagedesign/{id}` 只返回发布快照 |

Boot/Cloud 菜单种子均包含 `publish/rollback/template` 权限。所有管理端 PageDesign 端点必须显式声明 `@SaCheckPermission`；App 发布读取与短期预览保持公开，但仍由租户上下文隔离。

装修公开读取的匿名白名单同时覆盖 `/upms/app/tenant/shop-info`、`/product/app/**`、`/promotion/app/pagedesign/**`、`/promotion/app/groupbuy/activity/**` 和 `/promotion/app/groupbuy/record/page`。Cloud 白名单存于 `db/cloud/3aryn_nacos.sql`，Boot 白名单存于 `aryn-boot/src/main/resources/application.yml`，新增公开读取接口时必须保持两处一致。

## 认证与公共头

| 请求头 | 管理端 | UniApp | 后端用途 |
|---|---|---|---|
| `satoken` | access store | authStore | Sa-Token 登录态 |
| `tenant-id` | 平台切租户时可设置 | 从 `VITE_TENANT_ID` 设置 | TenantContext/MyBatis 隔离 |
| `Accept-Language` | 设置 | 未统一发现 | 管理端国际化 |
| `platform-type` | 无 | H5/APP/WX_MA | ToC 平台登录和业务判断 |
| `app-id` | 无 | 微信小程序设置 | 小程序账号与支付配置 |

## 联调验证清单

1. 记录源码 URL、浏览器最终 URL、运行模式和 Gateway/Boot context。
2. 验证 token 类型、租户 ID 与平台头，不仅看 HTTP 200。
3. 验证业务响应 `code === 0` 和真实 data schema。
4. 写接口检查事务、幂等、逻辑删除和租户条件。
5. 动态菜单接口同时验证权限码、component 路径与租户套餐授权。
