// 自动生成的 API 代理代码
//
// 注意：此文件之前由 Alova 根据 Swagger Petstore 示例自动生成，与业务无关，已清理。
// 当前项目使用手动定义的 API 函数，不需要此文件。
// 如需恢复自动生成，请配置 alova.config.ts 指向实际的后端 OpenAPI 文档。

export function createApis(_alovaInstance: any, _config: any) {
  return {}
}

export const withConfigType = <T>(config: T): T => config
