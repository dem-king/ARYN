<?xml version="1.0" encoding="UTF-8" ?>
<!--
  Copyright (c) 2025 天启雨数科技有限公司
  All rights reserved.
  注意：
  本项目源代码由天启雨数科技有限公司原创开发，版权所有。
-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aryn.cloud.product.mapper.ProductCodeMappingMapper">

    <select id="selectActiveByCodeValues" resultType="com.aryn.cloud.product.api.entity.ProductCodeMapping">
        SELECT
        id, spu_id, sku_id, code_type, code_value, match_source, confidence, `status`, tenant_id,
        create_by, update_by, create_time, update_time, del_flag
        FROM product_code_mapping
        WHERE tenant_id = #{tenantId}
          AND del_flag = '0'
          AND `status` = '1'
          AND code_value IN
        <foreach collection="codeValues" item="codeValue" open="(" separator="," close=")">
            #{codeValue}
        </foreach>
    </select>

    <select id="selectByCodeIgnoreDelFlag" resultType="com.aryn.cloud.product.api.entity.ProductCodeMapping">
        SELECT
        id, spu_id, sku_id, code_type, code_value, match_source, confidence, `status`, tenant_id,
        create_by, update_by, create_time, update_time, del_flag
        FROM product_code_mapping
        WHERE tenant_id = #{tenantId}
          AND code_type = #{codeType}
          AND code_value = #{codeValue}
        LIMIT 1
    </select>

    <select id="searchActiveByKeyword" resultType="com.aryn.cloud.product.api.entity.ProductCodeMapping">
        SELECT
        id, spu_id, sku_id, code_type, code_value, match_source, confidence, `status`, tenant_id,
        create_by, update_by, create_time, update_time, del_flag
        FROM product_code_mapping
        WHERE tenant_id = #{tenantId}
          AND del_flag = '0'
          AND `status` = '1'
          AND (
            code_value LIKE CONCAT('%', #{keyword}, '%')
            OR code_type LIKE CONCAT('%', UPPER(#{keyword}), '%')
          )
        LIMIT 50
    </select>

</mapper>
