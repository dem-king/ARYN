<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aryn.cloud.promotion.mapper.SeckillActivityMapper">

    <sql id="seckillActivitySql">
        seckill_activity.id,
        seckill_activity.tenant_id,
        seckill_activity.activity_name,
        seckill_activity.start_time,
        seckill_activity.end_time,
        seckill_activity.status,
        seckill_activity.description,
        seckill_activity.create_by,
        seckill_activity.update_by,
        seckill_activity.create_time,
        seckill_activity.update_time,
        seckill_activity.del_flag,
        seckill_activity.version
    </sql>

    <select id="selectAdminPage" resultType="com.aryn.cloud.promotion.api.entity.SeckillActivity">
        SELECT
        <include refid="seckillActivitySql"/>
        FROM seckill_activity
        <where>
            AND seckill_activity.del_flag = '0'
            <if test="query.activityName != null and query.activityName != ''">
                AND seckill_activity.activity_name LIKE CONCAT('%', #{query.activityName}, '%')
            </if>
            <if test="query.status != null">
                AND seckill_activity.status = #{query.status}
            </if>
        </where>
        ORDER BY seckill_activity.create_time DESC
    </select>

</mapper>