# 2026-07-04 工作日志

## 代码审查修复项目 — P0 安全修复完成

根据四份代码审查报告（code-review-summary/backend/frontend/uniapp），进行了三端 P0 严重问题修复。

### 后端 Java P0（8项 ✅）
- **S01** 数据库密码明文 → 环境变量占位符 `${DB_PASSWORD}`（dev 带默认值，prod 强制配置）
- **S02** AES密钥硬编码 → 环境变量 + 新增 CBC 模式方法（保留 ECB 兼容旧数据）
- **S03** App API越权 → 6处增加 userId 归属校验（AppOrderInfoController/AppUserAddressController/AppOrderRefundController/AppOrderItemController）
- **S04** GET写操作 → PUT/DELETE（cancel→PUT, del→DELETE, receiver→PUT）
- **S05** cancelOrder分布式事务 → `@Transactional` 改 `@GlobalTransactional`
- **S06** 库存竞态 → Redisson MultiLock 分布式锁
- **S07** 支付回调事务 → 3个方法添加 `@Transactional(rollbackFor=Exception.class)`
- **S19** 登录信息泄露 → 统一返回"账号或密码错误" + ArynBusinessException

### 后端 Java P1（8项 ✅）
- **M03** SecurityUtils NPE → 修正 isWebRequest() 逻辑 + getUserId/getOpenId 空判断
- **M04** 全局异常处理器 → 屏蔽敏感信息，返回"系统繁忙"
- **M05** allowMultiQueries → 从 dev/prod 配置中移除
- **M06** SaToken is-share → false 改 true
- **M07** 订单创建幂等 → Redis SETNX + requestId 校验
- **M08** 支付回调去重窗口 → 10s 改 24h
- **M09/M10** 在 P0 中已修复

### UI 前端 P0（6项 ✅）
- 移除登录页硬编码凭据（仅开发环境显示提示）
- AES ECB → CBC 模式 + IV
- .env 变量名拼写修正（PRIVICE→PRIVATE）+ store 密钥加强
- v-html XSS → 新建 sanitize.ts 过滤工具
- VerifySlide 事件监听泄漏 → 具名函数 + onBeforeUnmount 清理
- dict store `&&`→`!_key` 逻辑修复

### UniApp P0（4项 ✅ + 清理）
- .env.production API 地址 localhost → 生产域名
- Token 明文存储 → 持久化字段过滤 + Base64 编码
- phoneLogin 手机号/验证码从 URL 移到 body
- wxLogin 类型 PhoneLoginParams → MPLoginParams
- 清理 Swagger Petstore 示例代码

### 预存编译问题（非本次引入）
- aryn-auth: `com.aryn.cloud.common.log.event` 包不存在
- aryn-order-biz: `SysLog` 类找不到（admin Controller 中引用）

### 待处理（移至后续阶段）
- 后端 P2: M01 异常规范化、M02 N+1查询优化、G01-G08 建议改进
- UI P1/P2: API类型定义、CRUD工厂、大文件拆分等
- UniApp P1/P2: 登录方法去重、API类型、region-picker拆分、vk-data-goods-sku-popup重构等
- 三端 P3: 测试补充、API版本管理、组件抽取等长期改进

---

## 第二轮修复 — P2/P3 全量推进完成

### 后端 Java P2（10项 ✅）
- **M02** N+1查询 → OrderItemMapper 新增 `selectByOrderIds` 批量查询方法
- **G01** @Valid → AppOrderInfoController.createOrder 添加 `@Valid`
- **G02** Result.fail 语义 → 新增 `failWithData(T)` 方法，标记 `fail(T)` 为 @Deprecated
- **G04** 日志级别 → Mapper 层 DEBUG → `${MAPPER_LOG_LEVEL:INFO}`，通过环境变量控制
- **G08** DTO校验注解 → CreateOrderDTO 添加 `@NotBlank/@NotEmpty` 注解

### UI 前端 P1（6项 ✅）
- **M08** CRUD工厂 → 新建 `crud-factory.ts`，重写 order-info.ts 作为范例
- **M10** request.ts URL重写 → `JSON.parse` → `=== 'true'`，`splice` → `slice`
- **M11** console.log清理 → 移除 forget-password/register/sys-server 3处调试日志
- **M13** auth store去重 → 抽取 `handleLoginSuccess` 公共方法，消除 50 行重复
- **M14** 全局错误处理 → bootstrap.ts 添加 `app.config.errorHandler` + unhandledrejection

### UI 前端 P2（2项 ✅）
- 移除未使用的 `getAccessCodesApi`
- 全局错误处理器已在 P1 中完成

### UniApp P1（8项 ✅）
- **M11** 登录去重 → 抽取 `executeLogin` 通用方法，4个登录方法从 ~120行 → ~4行
- **M12** 401/403去重 → 抽取 `handleUnauthorized` 统一方法
- **M13** console.log → authStore 中移除 emoji 日志
- **M16** VUE3死代码 → 移除 4 处 `#ifdef/#ifndef VUE3` 条件编译
- **M18** 分包预加载 → pages.json 添加 `preloadRule`
- **M19** 图片懒加载 → category 页面 image 标签添加 `lazy-load`
- **M22** ESLint → `no-console: 'off'` → `['warn', { allow: ['warn', 'error'] }]`
- **M25** customize-tab-bar → 删除空壳目录
- **M31** handlers.ts useGlobalToast → 移至 handleUnauthorized 内部调用

### UniApp P2（5项 ✅）
- **M23** DIY动态组件 → v-if链改为 `component :is` 动态组件 + 映射表
- **M24** storage工具 → 添加命名空间前缀 + 过期机制 + JSON封装
- **M25** dictStore → 数组改 `Record<string,any>` O(1)查找 + 修复空值检查
- **M32** instance.ts URL重写 → `splice` → `slice` + 添加注释
- **M30** 全局错误处理 → App.vue 添加 `onError` + `onUnhandledRejection`

### 三端 P3（2项 ✅）
- **A5** 统一防重提交AOP → 新建 `@PreventDuplicateSubmit` 注解 + `PreventDuplicateSubmitAspect` 切面
- 安全加固已完成（P0-P1中）

### 待后续专项处理
- 后端测试补充（需团队协作，2周工作量）
- API版本管理引入（需全栈协调）
- region-picker.js 14758行拆分（需数据迁移方案）
- vk-data-goods-sku-popup 重构为 Composition API（1373行，高风险）
- UI 5个超大文件拆分（需逐个拆分，2-3天）
- MQ消费可靠性保障（需RocketMQ配置调整）
