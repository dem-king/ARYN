# 增量开发待办清单 — 第一波 / 第二波缺口补齐

> 编制时间：2026-07-06
> 依据：`2026-07-05-increment-dev-plan.md` + 代码逐条核实（仅分析，未改代码）
> 说明：✅ 已完成项无需处理；以下为缺口与待办。

---

## 🔴 P0 阻断性（不修无法运行）

### F2-02 会员等级增强 — 数据库 schema 未建表
实体 / Service / 前端页均已写好，但 SQL 无对应建表 / ALTER，运行时必报"表或字段不存在"。

| # | 待办 | 位置 / 证据 |
|---|------|------------|
| 1 | `member_level` 表 ALTER 扩展列：`growth_value`、`is_paid`、`price`、`duration`、`exclusive_discount`、`birthday_gift_points` | 实体 `MemberLevel.java:48-63` 已定义；建表在 `db/cloud/4aryn_user.sql`，**无扩展列 DDL** |
| 2 | 新建 `member_growth_log` 表（成长值变动记录） | 实体 `MemberGrowthLog.java` 已存在，**无建表 SQL**（`MemberLevelServiceImpl.addGrowthValue` 写入该表） |
| 3 | 新建 `member_paid_order` 表（付费会员订单） | 实体 `MemberPaidOrder.java` 已存在，**无建表 SQL** |
| 4 | `coupon_info` 表 ALTER 加 `member_level_limit` 列（专享券） | 实体 `CouponInfo.memberLevelLimit` 已存在（:86），**无列 DDL** |
| 5 | C 端商品详情 / 列表按当前会员等级返回 `member_price` 专属价 | `GoodsSpu.memberPrice` 字段存在（:122），但 app 层与 `GoodsSpuServiceImpl` 均未引用，专属价未生效 |

---

## 🟡 P1 功能性缺失（部分实现）

### F1-04 多端小程序矩阵
构建脚本 7 平台齐全，但仅微信有真实 appid，且登录 / 支付只覆盖 3 个平台。

| # | 待办 | 位置 / 证据 |
|---|------|------------|
| 6 | 申请并填入各平台真实 appid（`alipay/baidu/toutiao/qq/kuaishou/lark` 当前为 `YOUR_*_APPID` 占位） | `aryn-mall-uniapp/manifest.config.ts:77/87/94/101/108/115` |
| 7 | 补齐百度 / QQ / 快手 / 飞书 的登录适配（`uni.login` + 登录按钮 + 后端 code→openid） | 现有仅 `MP-WEIXIN/MP-ALIPAY/MP-TOUTIAO`：`pages/login/components/QuickLogin.vue`、`pages/login/index.vue` |
| 8 | 补齐上述 4 平台支付适配 | `src/utils/pay.ts` 仅有 `MP-WEIXIN(wxpay)/MP-ALIPAY(alipay)/MP-TOUTIAO/H5/APP-PLUS` 分支 |
| 9 | 订阅消息 `requestSubscribeMessage` 条件编译排查（其余平台是否需降级） | 前端未检索到该 API 调用 |
| 10 | 真机验证抖音 + 支付宝主流程（登录 / 支付 / 下单） | 规划验收标准① |

---

## 🟢 P2 确认 / 收尾（低风险）

### F1-01 暗色主题
| # | 待办 | 位置 / 证据 |
|---|------|------------|
| 11 | 确认默认主题：规划推测为 `light`，实际 `config.ts:115` 默认 `dark`；如不符合预期，在 `apps/web-ele/src/preferences.ts` 覆盖 `theme.mode` | `packages/@core/preferences/src/config.ts:115` |

---

## ✅ 已完成项（无需处理）

| 功能 | 状态 | 关键证据（节选） |
|------|------|----------------|
| F1-02 Excel 导入导出 | ✅ | `aryn-common-excel` + `ExcelUtils` + easyexcel 3.3.4；订单/会员/商品 `/export`；商品 `/import` + `GoodsSpuImportListener` 自动建分类/品牌 |
| F1-03 商品品牌管理 | ✅ | `pms_brand` 表 + `Brand` 实体/DTO/VO + `BrandController`/`AppBrandController` + SPU `brand_id` + 详情回填品牌名 |
| F1-05 数据大屏 | ✅ | 后端 4 聚合接口 + 前端 `views/visual/screen/index.vue`（1920×1080 + scale + 翻牌器/趋势/排行/漏斗 + 全屏路由 + 30s 轮询） |
| F2-01 积分商城 | ✅ | `PointsGoods` 实体 + 管理/C 端 CRUD/兑换；`exchange/{id}` 事务内 Dubbo 扣积分 + 乐观锁扣库存 + `PointsExchangeRecord` + 优惠券写入 |
| F2-03 Meilisearch 搜索 | ✅ | `docker-compose.yml` meilisearch:v1.6 + `aryn-common-search` 模块 + `SPU_INDEX` + RocketMQ 异步同步 + `/search/rebuild` + C 端 `/search` `/suggest` + 前端联想/历史/热搜/分面 |

---

## 建议执行顺序
1. **先做 P0（F2-02 的 5 项）** —— 纯 DDL + C 端专属价返回，工作量小但阻断功能，建议独立一个补丁 PR。
2. **再做 P1（F1-04 的 6-10 项）** —— 需各平台 appid 申请（外部依赖，提前排期）+ 登录/支付适配 + 真机验证。
3. **P2（F1-01 第 11 项）** 顺手确认即可。

> 完成上述后，第一波 5 项、第二波 3 项方才真正"做完整"。
